
const user_path = 'https://jsonplaceholder.typicode.com/users';
const provider_path = 'http://localhost:3000';

export const environment = {
  production: false,
  apiUrl: `${user_path}`
};

export const ProviderEnvironment = {
    production: false,
    apiUrl: `${provider_path}/api/providers`
};

